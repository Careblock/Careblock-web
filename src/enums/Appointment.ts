export enum APPOINTMENT_STATUS {
    ACTIVE = 1,
    POSTPONED = 2,
    REJECTED = 3,
    CHECKEDIN = 4,
}

export enum APPOINTMENT_STATUS_NAME {
    ACTIVE = 'Active',
    POSTPONED = 'Postponed',
    REJECTED = 'Rejected',
    CHECKEDIN = 'CheckedIn',
}
